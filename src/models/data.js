// Centralized tracker for loading state

const initialState = {
  pendingRequests: 0,
}

export const SET_LOADING_STATE = 'SET_LOADING_STATE'

export default {
  state: initialState,
  getters: {},
  actions: {},
  mutations: {}
}

// Actions
